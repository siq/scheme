components:
  - name: scheme
    description: Schema is a declarative schema framework for python.
    dependencies:
      - python
      - pyyaml
    builds:
      default:
        script: |
          python=$(find -L $BUILDROOT -type f -executable -name python)
          $python setup.py install
